"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = getTransactionLink;

var _constants = require("../../../constants");

/**
 * getRawTransactionLink
 *
 * Builds and returns the raw transaction link
 *
 * @param transactionId
 * @param chainObject
 * @param getRawVersion
 * @returns {*}
 */
function getTransactionLink(transactionId, chainObject, getRawVersion = false) {
  if (!transactionId || !chainObject) {
    return '';
  }

  const rawTransactionLinkTemplate = chainObject.transactionTemplates[getRawVersion ? 'raw' : 'full'];
  return rawTransactionLinkTemplate.replace(_constants.TRANSACTION_ID_PLACEHOLDER, transactionId);
}