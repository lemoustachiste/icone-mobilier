"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = generateRevocationReason;

var _useCases = require("../../i18n/useCases");

function generateRevocationReason(reason) {
  reason = reason.trim();
  reason = reason.length > 0 ? ` ${(0, _useCases.getText)('revocation', 'preReason')} ${reason}${reason.slice(-1) !== '.' ? '.' : ''}` : '';
  return `${(0, _useCases.getText)('revocation', 'reason')}${reason}`;
}