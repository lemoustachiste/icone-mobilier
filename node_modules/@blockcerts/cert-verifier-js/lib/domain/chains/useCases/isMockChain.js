"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = isMockChain;

var _constants = require("../../../constants");

function isMockChain(chain) {
  if (chain) {
    const chainCode = typeof chain === 'string' ? chain : chain.code; // TODO: can it be string?

    const isChainValid = Object.keys(_constants.BLOCKCHAINS).some(chainObj => chainObj === chainCode);

    if (!isChainValid) {
      return false;
    }

    return !!_constants.BLOCKCHAINS[chainCode].test;
  }

  return false;
}